"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault").default;

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

var _resolveFocusColor = _interopRequireDefault(require("./resolveFocusColor"));

var _resolveAccentColor = _interopRequireDefault(require("./resolveAccentColor"));

var _consts = require("../../consts");

const resolveFillColor = ({
  type,
  theme,
  selected,
  focus
}) => {
  if (focus) {
    if (type === _consts.TYPES.UNAVAILABLE) return "";

    if (!selected) {
      return _resolveAccentColor.default;
    }

    return _resolveFocusColor.default;
  }

  if (type === _consts.TYPES.LEGROOM) return selected ? theme.orbit.paletteBlueNormal : theme.orbit.paletteBlueLight;
  if (type === _consts.TYPES.UNAVAILABLE) return theme.orbit.paletteCloudLight;
  return selected ? theme.orbit.paletteProductNormal : theme.orbit.paletteProductLight;
};

var _default = resolveFillColor;
exports.default = _default;