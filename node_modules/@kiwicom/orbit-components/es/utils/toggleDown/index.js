import { keyframes } from "styled-components";

const toggleDown = contentHeight => keyframes(["0%{max-height:0;opacity:0;overflow:hidden}100%{max-height:", "px;opacity:1;overflow:visible}"], contentHeight);

export default toggleDown;