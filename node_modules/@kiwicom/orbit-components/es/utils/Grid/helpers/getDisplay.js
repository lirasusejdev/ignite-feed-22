import { css } from "styled-components";
import { isDefined } from "../../layout";

const getDisplay = (inline, force) => css(["display:", ";"], (isDefined(inline) || force) && (inline ? "inline-grid" : "grid"));

export default getDisplay;