import { css } from "styled-components";
import mq from "../../utils/mediaQuery";
import defaultTheme from "../../defaultTheme";
import { getBorder } from "./borders";
export const CardElement = css(["width:100%;box-sizing:border-box;position:relative;box-shadow:", ";border-top:", ";background:", ";", ""], ({
  expanded,
  theme
}) => expanded && theme.orbit.boxShadowActionActive, ({
  expanded
}) => !expanded && getBorder, ({
  theme
}) => theme.orbit.backgroundCard, mq.largeMobile(css(["border-left:", ";border-right:", ";"], ({
  expanded
}) => !expanded && getBorder, ({
  expanded
}) => !expanded && getBorder)));
CardElement.defaultProps = {
  theme: defaultTheme
};