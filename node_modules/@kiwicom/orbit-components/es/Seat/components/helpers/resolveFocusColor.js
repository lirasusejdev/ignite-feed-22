import { TYPES } from "../../consts";

const resolveFocusColor = ({
  type,
  theme,
  selected
}) => {
  if (!selected) return "";
  return type === TYPES.LEGROOM ? theme.orbit.paletteBlueNormalHover : theme.orbit.paletteProductNormalHover;
};

export default resolveFocusColor;